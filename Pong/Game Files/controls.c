/* #include <stdio.h>
#include <math.h>
#include <stdlib.h>

int get_paddle_velocity(int x, int y)
{
	
}
int main(void) 
{
		int p1_xy = joy_one.cmdGetPosition()
		int p2_xy = joy_two.cmdGetPosition()
		paddle_one_velocity = get_paddle_velocity(x, y) 
		paddle_two_velocity = get_paddle_velocity(x, y)
} */
